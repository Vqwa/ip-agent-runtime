e2b
mcp
ddgs
oxylabs-ai-studio
fal-client==0.13.1
exa-py
firecrawl-py
parallel-web
pyjwt[crypto]
uvicorn[standard]
modal==1.3.4
daytona==0.155.0
# Build backend for the editable install of the local package (hash-pinned so the
# Dockerfile can build --no-build-isolation; otherwise pip fetches it unhashed).
setuptools>=77,<83
wheel
